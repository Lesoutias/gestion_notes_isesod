# En développement local, laisser vide : le proxy Vite redirige /api vers le backend.
# Définir uniquement si le frontend et l'API sont sur des domaines distincts (ex. production).
# VITE_API_URL=http://localhost:8004
